services:
  vintage_story:
    image: neebz/vintage-story-server:2026.05.10-1
    ports:
      - "42420:42420/tcp"
      - "42420:42420/udp"
    volumes:
      - ${GAME_FILES_LOCATION}/server_files:/app/server_files
      - ${GAME_FILES_LOCATION}/game_files:/app/game_files
    environment:
      VERSION: ${VERSION}
      CHANNEL: ${CHANNEL}
    user: 1000:1000